import Layout from "@/Layout";
import UsersList from "@/components/users/UsersList";
import React from "react";

const Chat = () => {
  return (
    <Layout>
      <UsersList />
    </Layout>
  );
};

export default Chat;
