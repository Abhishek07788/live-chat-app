import Layout from "@/Layout";
import ChatList from "@/components/Chat/ChatList";
import React from "react";

const Chat = () => {
  return (
    <Layout>
      <ChatList />
    </Layout>
  );
};

export default Chat;
